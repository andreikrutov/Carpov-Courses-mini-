Необходимо считать данные из таблицы Excel.  

Проблема в том, что внутри на листе несколько разрозненных таблиц,  
при этом одна таблица смещена влево относительно всех других.  
Реализован следующий алгоритм:  
- Ищем индексы строк с заголовками маленьких таблиц  
- Разбиваем большой df на маленькие по этим индексам  
- Смотрим в каком из них данные смещены по отношению в к дргуим  
- Переименовываем столбцы так, чтобы заголовки смеестились влево
- Выбрасываем строки с текстом (заголовки маленьких таблиц)  
- Объединяем получившиеся маленькие таблицы
